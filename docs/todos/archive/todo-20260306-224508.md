# TODO (2026-03-06) — 本地产物与遗留 JS 清理

- [x] T1 归档当前计划文件并建立清理阶段 plan/todo
- [x] T2 删除仓库内本地构建产物目录（`dist/`、`packages/cli/build/`）
- [x] T3 复查剩余 `*.js` 与 `dist/`，确认无源码遗留
- [x] T4 记录原因与结果

## 验收记录
- [x] 仓库内非 `node_modules` / 非 `.git` 路径下无 `dist/` 目录
- [x] 仓库内非 `node_modules` / 非 `.git` 路径下无 `packages/cli/build/`
- [x] 仓库内非 `node_modules` / 非 `.git` 路径下无 `*.js` 文件
- [x] `git status` 仅剩 `plan.md` / `todo.md` 与归档文件变更
