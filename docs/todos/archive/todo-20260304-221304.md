# Spanory TODO：OpenCode 自动读取环境与可诊断日志（2026-03-04）

## T1 阶段初始化
- [x] 归档旧 `plan.md` 与 `todo.md`
- [x] 写入本阶段 `plan.md` 与 `todo.md`

验收：
- [x] `ls -1 docs/plans/archive | rg '20260304-215959'`
- [x] `rg -n "OpenCode 自动读取环境|Acceptance" plan.md todo.md`

## T2 OpenCode 插件增强
- [x] 插件启动时自动加载 `~/.env`（仅补齐缺失环境变量）
- [x] 增加 `plugin.log` 关键诊断日志（endpoint 缺失、发送失败、去重跳过）
- [x] 保持现有 `plugin-status.json` 语义兼容

验收：
- [x] `rg -n "loadUserEnv|pluginLogFilePath|appendFile|endpoint_unset|fingerprint_unchanged" packages/opencode-plugin/src/index.js`

## T3 CLI 诊断增强
- [x] `runtime opencode plugin doctor` 增加日志路径检查
- [x] doctor 详情包含对 endpoint/状态字段的更清晰提示

验收：
- [x] `rg -n "opencode_plugin_log|plugin-status.json|endpointConfigured" packages/cli/src/index.js`

## T4 测试补齐
- [x] 新增单测：未设置进程环境但 `~/.env` 有配置时可发送
- [x] 新增单测：endpoint 缺失时写入诊断日志
- [x] 运行目标单测

验收：
- [x] `npm run --workspace @spanory/cli test -- test/unit/opencode.plugin.runtime.spec.js`

## T5 文档同步
- [x] 更新 `README.md`（补充 plugin.log 路径与排障命令）
- [x] 更新 `docs/README_zh.md`（补充 plugin.log 路径与排障命令）

验收：
- [x] `rg -n "plugin.log|opencode plugin doctor|未上报|not report|endpointConfigured" README.md docs/README_zh.md`
