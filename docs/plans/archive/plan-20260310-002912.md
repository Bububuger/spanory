# Plan (2026-03-10) — Context Events 扩展到 Opencode

## 目标
1. 将 `context` canonical 事件扩展到 `opencode` runtime。
2. 修复因新增 context spans 引起的插件/OTLP 测试断言漂移。
3. 同步 telemetry 字段契约 runtime_support。

## 执行顺序
1. 调整 `normalize.ts` 启用 runtime 列表（加入 opencode）
2. 更新 `normalize.spec.ts` runtime 覆盖
3. 跑 opencode/otlp 相关单测并修正受影响断言
4. 更新 `telemetry/field-spec.yaml` 的 context runtime_support
5. 跑 check + telemetry:check 验收

## 验收标准
- `opencode` 输出含 `agentic.event.category=context`
- `opencode.plugin.runtime.spec.ts` 通过
- `otlp.spec.ts` 通过
- `npm run --workspace @bububuger/spanory check` 通过
