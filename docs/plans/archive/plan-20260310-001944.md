# Plan (2026-03-10) — Context Score Shared Contract

## 目标
1. 将 `pollution_score_v1` 从 runtime 侧启发式实现下沉到 `packages/core`，形成共享参考实现。
2. CLI `normalize` 改为调用共享实现，避免语义分叉。
3. 保持现有行为与测试通过，不扩大 runtime 范围。

## 执行顺序
1. 在 `packages/core/src/index.ts` 导出 `pollutionScoreV1` 及输入类型
2. 在 `packages/cli/src/runtime/shared/normalize.ts` 对齐同公式（临时内联同名函数 + TODO 引导，避免跨包 rootDir 限制）
3. 增补 `packages/core` 单元测试，验证边界值
4. 跑 `core check/test` 与 `cli normalize` 回归

## 验收标准
- `packages/core` 暴露可复用 `pollutionScoreV1`
- CLI 使用与 core 一致的计算语义
- `npm run --workspace @bububuger/core check` 通过
- `npm run --workspace @bububuger/spanory check` 通过
- `cd packages/cli && npx vitest run test/unit/normalize.spec.ts` 通过
