# Plan (2026-03-09) — Context OTel 实现启动（MVP-A1/A2 首批）

## 目标
1. 在现有运行时归一化链路中新增 `context` 类 canonical 事件（先 Claude runtime）。
2. 打通 `agentic.context.*` 字段上报（snapshot / boundary / source_attribution）。
3. 为核心字段补单测，确保不破坏现有 OTLP 导出路径。

## 设计范围（本阶段）
1. `context_snapshot`：`estimated_total_tokens` / `fill_ratio` / `delta_tokens` / `composition` / `top_sources`
2. `context_boundary`：`boundary_kind` / `compaction_ratio`（基于 compact 信号 + token 跌落启发式）
3. `context_source_attribution`：`source_kind` / `source_name` / `token_delta` / `pollution_score` / `score_version`

## 执行顺序
1. 在 `normalize.ts` 新增 context 估算与事件组装（仅 `runtime=claude-code` 生效）
2. 在 `normalize.spec.ts` 增加 context 相关断言
3. 运行最小验收（单测）
4. 运行 `check`，确认类型与构建无回归

## 验收标准
- Claude 会话可产出 `agentic.event.category=context` 事件
- 上述 13 个 `agentic.context.*` 字段可观测到（根据事件类型分布）
- `npm run --workspace @bububuger/spanory check` 通过
- `cd packages/cli && npx vitest run test/unit/normalize.spec.ts` 通过
