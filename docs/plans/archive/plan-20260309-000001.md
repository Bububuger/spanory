# Plan

## 目标
修复 Codex notify 首发偶发把 Bash duration 记为 0ms，需要手工重发才能纠正的问题。

## 方案
1. 在 notify 脚本中保留实时首发，同时增加短延迟二次补发。
2. 更新 setup BDD 断言，确保生成脚本包含补发逻辑。
3. 重新安装本机 codex notify 配置并验证最新 session 自动纠正能力。

## 验收
- setup BDD 通过。
- 重新应用 codex setup 后，生成脚本包含 delayed replay。
- 真实新 turn 首发后无需手工命令即可把 Bash duration 更新为非零。
