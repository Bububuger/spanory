# Plan (2026-03-10) — Context Alert Metrics

## 目标
1. 在 alert evaluator 增加 context 相关 session 指标：
   - `context.unknown_delta_share.window5`
   - `context.unknown_top_streak`
   - `context.high_pollution_source_streak`
2. 增补单测覆盖这些指标
3. 运行 unit + check 验收

## 验收标准
- `alert.spec.ts` 覆盖并通过
- CLI 全量 unit 通过
- `@bububuger/spanory check` 通过
