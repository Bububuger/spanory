# Plan (2026-03-10) — Context Events 扩展到 Codex/Openclaw

## 目标
1. 将 `context` canonical 事件从 `claude-code` 扩展到 `codex` 与 `openclaw`。
2. 保持 `opencode` 暂不启用（降低 plugin 回归面）。
3. 更新受影响 golden/单测并完成最小回归。

## 执行顺序
1. 调整 `normalize.ts` 的 runtime 启用条件
2. 补充 `normalize.spec.ts`（codex/openclaw 也发 context 事件）
3. 更新 codex golden fixture
4. 运行 `normalize/codex/openclaw` 单测与 `check`

## 验收标准
- `codex` / `openclaw` 输出含 `agentic.event.category=context`
- `codex.adapter.golden.spec.ts` 全通过
- `npm run --workspace @bububuger/spanory check` 通过
