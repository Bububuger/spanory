# Plan

## 目标
修复 Codex notify hook 首发过早导致 Bash/Tool 时长被 0ms 上报的问题。

## 方案
1. 增加可复用的 transcript settle helper，按 mtime 稳定窗口等待文件落稳。
2. 在 codex hook 路径中接入 settle 逻辑，仅影响 codex notify 首发，不改 export/backfill。
3. 为 settle helper 增加 unit test，覆盖稳定/超时两种路径。
4. 运行最小单测与 codex 相关回归，确认无回退。

## 验收
- unit test 通过。
- codex 相关单测通过。
- 手工重发 codex hook 后，ClickHouse 中对应 TOOL/Bash 节点存在非零 duration。
