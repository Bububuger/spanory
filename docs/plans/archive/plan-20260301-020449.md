# Spanory Tool Count Inflation Fix Plan (2026-03-01)

## Goal
修复 Claude transcript 中 assistant 快照重复导致的 tool observation 放大问题，保证 tool 计数与真实调用一致。

## Scope
- `packages/cli/src/runtime/claude/adapter.js`
- `packages/cli/test/unit/adapter.spec.js`
- 新增 fixture

## Tasks
### T1 Assistant 快照去重
- 在 turn 内对 assistant 消息按 `message.id` 去重，采用 latest-wins。
- 去重后再做：turn output 拼接、usage 聚合、tool_use 提取。

验收：同一 `tool_call_id` 不再因快照重复而重复计数。

### T2 回归测试
- 新增带重复 assistant 快照的 fixture。
- 新增/更新 unit test：验证 tool 数不放大，usage 不被重复累计。

验收：unit test 通过且覆盖重复快照场景。

### T3 终验
- 运行 `npm run --workspace @spanory/cli test -- test/unit/adapter.spec.js`。

验收：通过。
