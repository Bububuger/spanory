# Plan (2026-03-11) — Execution 1-25（阶段1：契约对齐与归因核心）

## 目标
1. 对齐 `pollutionScoreV1` 与设计契约公式，消除 core/cli 双实现漂移。
2. 在 context canonical 事件中补齐估算与边界检测字段，统一 schema。
3. 在归因事件补齐 `source_share`、`repeat_count_recent`，支持四因子评分与告警。

## 执行顺序
1. core 契约修正：`packages/core/src/index.ts` + `packages/core/test/*`
2. cli 归因引擎修正：`packages/cli/src/runtime/shared/normalize.ts`
3. 测试与 golden 对齐：`packages/cli/test/unit/*` + fixtures
4. 验收命令：core test → cli unit test

## 验收标准
- `normalize.ts` 不再定义本地 `pollutionScoreV1`
- `source_kind=unknown` 仅增加 15 分惩罚（非 25）
- `context_source_attribution` 包含 `agentic.context.source_share` 与 `agentic.context.repeat_count_recent`
- `packages/core` 与 `packages/cli` 相关单测通过
