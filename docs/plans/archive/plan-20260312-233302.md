# Plan (2026-03-12) — alipay-cli 切换为 JS 包分发

## 目标
1. 将 `@alipay/spanory` 从平台二进制分发改为 JS CLI 分发。
2. 保持安装入口与执行行为和 `@bububuger/spanory` 一致（`bin -> dist/index.js`）。
3. 删除/停用二进制准备链路，避免机型分发耦合。

## 执行顺序
1. 修改 `packages/alipay-cli/package.json`（files/bin/scripts）。
2. 清理 `packages/alipay-cli/bin` 和 `scripts/prepare-bin.sh` 的二进制耦合逻辑。
3. 执行构建与基础执行校验。

## 验收标准
- `@alipay/spanory` 包元数据不再声明平台二进制文件
- `bin.spanory` 指向 `dist/index.js`
- 本地可通过 node 执行 dist CLI 并输出帮助信息
