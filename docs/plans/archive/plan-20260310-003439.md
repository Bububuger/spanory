# Plan (2026-03-10) — Core Canonical Context Contract

## 目标
1. 在 `packages/core` 增补 context canonical 合同类型：snapshot/boundary/source_attribution。
2. 导出完整 source taxonomy 常量与类型，统一运行时语义。
3. 增补 core 测试，覆盖类型相关常量与评分函数兼容行为。

## 执行顺序
1. 更新 `packages/core/src/index.ts`：新增 context 类型与 source enum 常量
2. 更新 `packages/core/test/*`：新增 taxonomy/contract 断言
3. 运行 `@bububuger/core check/test`
4. 运行 `@bububuger/spanory check` 保障无连锁回归

## 验收标准
- core 可导出 context canonical 合同类型与 source taxonomy
- core check/test 通过
- cli check 通过
