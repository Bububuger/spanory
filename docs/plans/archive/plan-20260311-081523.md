# Plan (2026-03-11) — Execution 1-25（阶段2：估算策略与回放能力）

## 目标
1. 实现 token 估算三级策略：usage / heuristic / calibrated。
2. 将估算能力上收至 core，供 runtime 共享。
3. 验证 context 字段在 golden + 单测中的稳定输出。

## 执行顺序
1. 在 `packages/core` 增加估算与校准工具函数及单测。
2. 在 `packages/cli` 的 normalize 接入核心估算与校准状态。
3. 更新 unit/golden fixtures。
4. 执行 core/cli 测试验收。

## 验收标准
- 中文/JSON/代码估算不再固定 chars/4
- `estimation_method` 可出现 `usage|heuristic|calibrated`
- 有 usage 锚点后，无 usage turn 可输出 `calibrated`
- core 与 cli 测试均通过
