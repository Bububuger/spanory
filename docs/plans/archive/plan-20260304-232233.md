# Spanory 金标测试扩展 Plan（合并 main 后能力/缺陷覆盖）(2026-03-04)

## Goal
对合并自 `main` 的新能力与 bugfix 补齐金标测试，确保“同输入同输出”覆盖不止于 OTLP。

## Scope
- `packages/cli/src/runtime/codex/adapter.js`（仅作为被测对象，不改行为）
- `packages/cli/test/fixtures/golden/codex/*`（新增）
- `packages/cli/test/unit/codex.adapter.golden.spec.js`（新增）
- `packages/cli/package.json`
- `docs/standards/test-baseline-spec.md`
- `docs/standards/regression-cases.md`
- `plan.md` / `todo.md`

## Tasks
### T1 阶段归档与计划切换
- 归档上一阶段 `plan/todo`。
- 写入本阶段目标与验收。

### T2 Codex 能力金标覆盖
- 新增 codex 金标输入样例：`session-a/session-b/session-open`。
- 新增 codex 金标 expected 生成脚本。
- 新增 codex 金标单测，固定比较稳定投影输出。

### T3 规范与台账同步
- 更新测试规范中的“当前金标覆盖范围”。
- 更新缺陷回归台账，补齐 codex turn completion 回归映射。

### T4 验收
- 刷新 codex 金标 expected。
- 执行 codex 相关 unit + golden 测试。

## Acceptance
1. `npm run --workspace @spanory/cli test:golden:update`
2. `cd packages/cli && npx vitest run test/unit/codex.adapter.golden.spec.js test/unit/codex.adapter.spec.js`
