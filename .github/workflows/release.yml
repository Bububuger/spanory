name: Release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

jobs:
  verify:
    name: Verify Before Release
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      - name: Check
        run: npm run check

      - name: Telemetry Gate
        run: npm run telemetry:check

      - name: Build
        run: npm run build

      - name: Unit Tests
        run: npm test

      - name: Coverage Gate
        run: npm run --workspace @bububuger/spanory test:coverage

      - name: BDD Tests
        run: npm run test:bdd

  build-binaries:
    name: Build ${{ matrix.artifact_name }}
    needs: verify
    runs-on: ${{ matrix.os }}
    timeout-minutes: 25
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            artifact_name: spanory-linux-x64
            artifact_path: dist/spanory-linux-x64
            build_command: npm run --workspace @bububuger/spanory build:bin:linux-x64
            smoke_command: ./dist/spanory-linux-x64 --help
          - os: macos-14
            artifact_name: spanory-macos-arm64
            artifact_path: dist/spanory-macos-arm64
            build_command: npm run --workspace @bububuger/spanory build:bin:macos-arm64
            smoke_command: ./dist/spanory-macos-arm64 --help
          - os: macos-14
            artifact_name: spanory-macos-x64
            artifact_path: dist/spanory-macos-x64
            build_command: npm run --workspace @bububuger/spanory build:bin:macos-x64
            smoke_command: ./dist/spanory-macos-x64 --help
          - os: windows-latest
            artifact_name: spanory-win-x64.exe
            artifact_path: dist/spanory-win-x64.exe
            build_command: npm run --workspace @bububuger/spanory build:bin:win-x64
            smoke_command: .\\dist\\spanory-win-x64.exe --help

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm

      - name: Install
        run: npm ci

      - name: Sync CLI Version With Tag
        if: startsWith(github.ref, 'refs/tags/v')
        run: npm version --no-git-tag-version "${GITHUB_REF_NAME#v}" --workspace @bububuger/spanory

      - name: Build Binary
        run: ${{ matrix.build_command }}

      - name: Binary Smoke
        run: ${{ matrix.smoke_command }}

      - name: Upload Binary Artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_path }}
          if-no-files-found: error

  publish-npm:
    name: Publish NPM Package
    needs: verify
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    environment: release
    timeout-minutes: 15
    env:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Node
        uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
          registry-url: https://registry.npmjs.org

      - name: Install
        run: npm ci

      - name: Sync CLI Version With Tag
        if: env.NPM_TOKEN != ''
        run: npm version --no-git-tag-version "${GITHUB_REF_NAME#v}" --workspace @bububuger/spanory

      - name: Build Before Publish
        run: npm run build

      - name: Publish CLI To npm
        if: env.NPM_TOKEN != ''
        run: npm publish --workspace @bububuger/spanory --access public
        env:
          NODE_AUTH_TOKEN: ${{ env.NPM_TOKEN }}

      - name: Skip NPM Publish (NPM_TOKEN missing)
        if: env.NPM_TOKEN == ''
        run: echo "NPM_TOKEN is not configured; skip npm publish."

  publish-release:
    name: Publish GitHub Release
    needs: build-binaries
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Download Build Artifacts
        uses: actions/download-artifact@v6
        with:
          pattern: spanory-*
          merge-multiple: true
          path: dist

      - name: Package Release Assets
        run: bash scripts/release/package-release-assets.sh "${{ github.ref_name }}"

      - name: Publish Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          generate_release_notes: true
          files: |
            dist/release/spanory-*.tar.gz
            dist/release/spanory-*.zip
            dist/release/SHA256SUMS.txt
